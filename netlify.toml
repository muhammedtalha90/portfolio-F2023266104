[build]
  command = "pip install -r requirements.txt && python manage.py migrate && python manage.py seed_portfolio && python manage.py collectstatic --noinput"
  publish = "staticfiles"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
